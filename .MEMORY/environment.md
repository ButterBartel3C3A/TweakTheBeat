---
title: 环境事实
type: environment
updated: 2026-09-22
---

# 环境事实（直接修正为最新事实）

## 仓库

- 工作仓库：`C:\Users\szsm\Documents\Github\TweakTheBeat`（GitHub，main 分支；2026-09-22 新建，含 README/需求文档/.MEMORY）。
- 外部数据源（本地，保密）：用例文档（205 用例速查表、测试矩阵）与固件参考源码。
  - 其名称/路径/内部结构**禁止写入仓库任何文本**（保密约定见 `.MEMORY/README.md`）；本地路径只存本机会话个人记忆（不推送）。
  - 使用方式：直接引用、不复制进仓库（D1 已决）；执行器运行时按会话个人记忆中的路径读取。
- 产物目录：待定（见 decisions.md D6 代码布局）。`.claude/skills/`（gitignored，待建）。

## 仓库约束（2026-09-22 更新）

- 默认不 commit/push/修改仓库既有文件（原始需求约束）。
- 例外：`.MEMORY/` 须提交推送（用户指示 #2）。
- gitignore 由用户持续更新；AI 等用户指定后再动。
- 测试对象为真实硬件，一次只连一台节点。

## 运行环境

- Windows 10 Pro（19045）；Python + bleak；无 GUI；第三方依赖尽量少。

## 目标设备协议要点（首个 profile；具体 UUID/广播名/帧值以本机需求文档与会话个人记忆为准，不写入仓库）

- 广播名：以本地文档为准；设备未连接约 32s 后休眠，扫描不到需人工唤醒。
- 服务与特征：1 个业务服务；下行特征（Write Without Response，≤20B，默认 MTU 23）；上行特征（Notify 无 Read，≤16B），需先写 CCCD 使能通知。
- **连接后 5s 内必须下发首个配置帧（17 字节），否则设备断开重广播；重连后需重新握手。**
- 设备单连接。
- 下行：首字节命令号；配置帧固定 17B；工具指令 2B；另有 2/4B 指令。
- 上行：首字节响应号，长度 3–12B 不等（含 float 响应与错误响应）；多字节大端。
- 位域细节与全部用例帧以本地用例速查文档与固件参考源码为准，禁止自行猜测。
