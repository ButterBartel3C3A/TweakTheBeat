---
title: 需求迭代日志
type: iteration
updated: 2026-09-23
---

# 需求迭代日志（时间倒序，只追加不改写）

## 2026-09-23 — D10 真机冒烟达成 + long-run 诉求（daemon 候选）

- 用户指示 #6：「接下来我们来测试一下。你将指导我对目标调试设备进行操作」——AI 主导一次性全量冒烟，用户配合物理操作（旅行锁唤醒、按住按键）。
- 冒烟结果：P0 八用例 7 PASS / 1 MANUAL（纯物理观察 A1.1）/ 0 FAIL；人类与 AI（--json）模式全流程验证通过。报告在本机 .local/runs/smoke.md。
- 冒烟暴露 6 处框架问题已修复（commit 99116fb）：bleak 3.x services 迭代、上行 t_ms、connect 输出 handshake 证据、--id 可重复、有规则无断言的纯物理用例判 MANUAL（对齐 D7 判定矩阵）、回归断言。
- 用户提议「能否 long run terminal 避免反复重新配对」——AI 澄清：目标设备无配对环节，32s 无连接休眠需旅行锁唤醒（唤醒≠配对）；repl 已支持持久连接，AI 模式长连接需 daemon 子命令（后台持连接+命令通道），列为 D11 待决。
- 两处设备行为与文档出入、一项新协议观测（触发后周期性步进进度上报）——细节记入本机 .local/README.md，仓库文本不涉帧值。

## 2026-09-22 — 用户指示 #5：设计定稿落地 + gitignore 分工

- 用户对五份设计草案（目录结构/profile TOML/断言规则/--json schema/SKILL 结构）全部无异议；AI 已将其写入 `.MEMORY/design.md`（设计唯一事实来源，脱敏版）。
- 用户指示「gitignore 由你维护，由我审阅并手动修改」——取代指示 #2 的「gitignore 由用户管理」。
- AI 已创建仓库根 `.gitignore`：敏感文件（需求文档）、`.local/`、Python 产物、`.claude/`（仅豁免 `.claude/skills/ble-cli/`），待用户审阅。
- 阶段 0（需求迭代）收官：11 项决策全部已决（D7 由草案定稿移入已决区），进入阶段 1 实现。

## 2026-09-22 — 用户指示 #4：设备/品牌脱敏 + 保持公开 + 授权历史重写

- 仓库保持公开；公开文本对设备/品牌脱敏：一律以代号「目标设备」指代被测设备，不出现品牌/产品名/广播名/协议 UUID/帧值（细则见 `.MEMORY/README.md` 保密约定）。
- 授权 AI 执行 amend + 强推，清理远程历史中 .MEMORY 的泄露内容（名称/路径/品牌名）。
- D1 已决：数据源「仅直接引用」+ 保密（见 decisions.md）。

## 2026-09-22 — 用户指示 #3：外部数据源保密（硬性）

- 外部数据源（用例文档与固件参考所在库）为商用闭源，仅限本机测试使用，不得让无关人员知晓其存在。
- 其名称/路径/内部结构**不得出现在任何推送云端的文本**（仓库文件、commit message 均在内），也不得将其内容推送云端。
- 本地路径只存本机会话个人记忆；仓库文本一律以「外部数据源（本地）」代称（约定见 `.MEMORY/README.md` 保密约定）。
- 已执行：AI 清理 .MEMORY 中此前写入的名称与路径，重写历史后强推。
- 发现（待用户决定）：需求原文 `ble-automation-prompt.md` 内含本地路径引用（在 initial commit 中已推送）；TweakTheBeat GitHub 仓库经查为公开（未登录可访问）。

## 2026-09-22 — 用户指示 #2：项目记忆入仓库

- 项目相关记忆统一保存到仓库内 `.MEMORY/` 文件夹（本文件夹），文件格式由 AI 合理设计并维护。
- 目的：保证项目可跨 agent 操作/维护（任何会话凭仓库内容恢复上下文）。
- `.MEMORY/` 需要 commit 并推送至 repo，作为协作的一部分。
- 除 `.MEMORY/` 外，仓库内原则上不保存与项目无关的东西。
- gitignore 由用户持续更新指定；AI 不擅自添加忽略规则，等用户指定。

## 2026-09-22 — 用户指示 #1：先迭代需求，不实现

- 用户要求先阅读 `ble-automation-prompt.md`，但**不开始实现**，通过长对话持续迭代需求后再动手。
- AI 已阅读需求全文、核实环境（工作仓库/数据源路径/固件源码位置均存在）、提出迭代讨论点（见 decisions.md 待决 D1–D10）。
- 需求第八节的五个设计问题（技术栈/四层架构/profile 格式/JSON schema/SKILL 草案）尚未正式回答，等用户发令。

## 2026-09-22 — 需求文档创建

- `ble-automation-prompt.md` 放入工作树（未跟踪、未提交，勿推送）。
